#include <iostream>

  int main()
{
  // modifiers are signed by default ( can hold both + and - values )
  
  unsigned long int x = 4294967295;
  std::cout << " The value of an unsigned long integer variable is: " << x;
}
